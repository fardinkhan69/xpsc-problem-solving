#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--)
    {
        int a,b;
        cin>>a>>b;
        //test case mile gese kore but I really don't have any clear idea
        int ans = a^b;
        cout<<ans<<endl;
    }
    return 0;
}